Write tests for the specified target in this repository using Vitest.

Checklist:
- Follow existing style: `describe` / `it` / `expect`.
- Cover normal cases and edge cases (`null`, zero division, empty arrays).
- Keep tests deterministic and fast.
- For route tests, follow existing request pattern used in this repo.
- Run relevant test commands and summarize pass/fail clearly.

References:
- `src/services/financialService.test.ts`
- `src/services/stockService.test.ts`
- `src/routes/*.test.tsx`
